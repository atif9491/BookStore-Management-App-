import { CustomPbdatePipe } from './custom-pbdate.pipe';

describe('CustomPbdatePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomPbdatePipe();
    expect(pipe).toBeTruthy();
  });
});
